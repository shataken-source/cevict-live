# Moltbook Viewer — copy to .env.local and fill in the values below.
# Get API keys from https://www.moltbook.com (register an agent, get key after human claims).

# =============================================================================
# Multiple agents (recommended — most people use several projects)
# =============================================================================
# One JSON array: each item has "label" (dropdown name) and "key" (that agent's Moltbook API key).

MOLTBOOK_AGENTS_JSON=[{"label":"PASTE_LABEL_1","key":"moltbook_sk_PASTE_KEY_1"},{"label":"PASTE_LABEL_2","key":"moltbook_sk_PASTE_KEY_2"}]

# Example (replace with your real labels and keys):
# MOLTBOOK_AGENTS_JSON=[{"label":"PetReunion","key":"moltbook_sk_abc123..."},{"label":"Progno","key":"moltbook_sk_def456..."}]

# =============================================================================
# Optional: One agent only
# =============================================================================
# Use this instead if you only have one Moltbook agent. Comment out MOLTBOOK_AGENTS_JSON above.

# MOLTBOOK_API_KEY=moltbook_sk_PASTE_YOUR_KEY_HERE

# =============================================================================
# Optional: Brief tab & last-run status
# =============================================================================
# Path to a daily brief markdown file (absolute or relative to app root). Shown in the Brief tab.
# MOLTBOOK_BRIEF_PATH=../petreunion/daily_brief.md

# Path to the trigger file written by your scheduled Moltbook check. Used for "Last updated" in the viewer.
# MOLTBOOK_TRIGGER_PATH=../petreunion/MOLTBOOK_TRIGGER.txt

# Path to the agent TODO list JSON file. Human adds from viewer; agent reads same file. Default: ./moltbook-todos.json
# MOLTBOOK_TODOS_PATH=./moltbook-todos.json

# Optional: Help chat (?) — answers questions about the viewer using the app docs. Requires OpenAI.
# OPENAI_API_KEY=sk-...
