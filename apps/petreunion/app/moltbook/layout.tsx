import { ReactNode } from 'react';

export const metadata = {
  title: 'Moltbook activity | PetReunion',
  description: 'Keep up with what your AI is working on. Your AI’s Moltbook posts and replies in one place — not for spying, for staying in the loop.',
};

export default function MoltbookLayout({ children }: { children: ReactNode }) {
  return children;
}
