/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // PWA support
  // Note: For full PWA, you'd use next-pwa package
  // This is basic manifest support
}

export default nextConfig
