# ─── Accu Solar — Environment Variables ──────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.

# ─── Supabase (optional — for historical data logging) ───────────────────────
# Create a free project at https://supabase.com
# Each user/installation needs their own project.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── OpenAI (optional — enables AI chat in the AI Setup tab) ─────────────────
# Get a key at https://platform.openai.com/api-keys
# Without this key, the AI tab uses the built-in knowledge base only.
NEXT_PUBLIC_OPENAI_KEY=sk-...

# ─── WeatherAPI (optional — higher quality weather data) ─────────────────────
# Free tier at https://www.weatherapi.com
# Without this key, the app falls back to Open-Meteo (free, no key needed).
NEXT_PUBLIC_WEATHERAPI_KEY=your_key_here
