# Praxis reference

Reference and docs area for **Praxis** (the main app lives in `apps/praxis`).

Use this folder for design notes, copy, or reference material. The running app is `apps/praxis` (port 3002).
