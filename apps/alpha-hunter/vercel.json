{
  "framework": "nextjs",
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "ignoreCommand": "git fetch origin $VERCEL_GIT_PREVIOUS_SHA --depth=1 2>/dev/null; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/alpha-hunter/ 2>/dev/null || exit 1"
}
